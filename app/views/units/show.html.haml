- unit_saved = @unit.likes.where(user: current_user).present?
.saved-container{ style: (unit_saved ? 'background: #64c879;' : 'background: #ff5353;'), data: { url: like_unit_path } }
  %p Save $20K CAD
  - if unit_saved
    %h1 + Added to Saved
  - else
    %h1 + Add to Saved
